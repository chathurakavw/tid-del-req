import { FC } from 'react';
import { IActionedItemSlice } from 'features/actioned/config/actioned-type';

/**
 *
 */
const ActionedItemSlice: FC<IActionedItemSlice> = () => {
    return (
        <div className="actioned-item-slice-wrapper">
            <div className="section">ActionedItemSlice</div>
        </div>
    );
};

export default ActionedItemSlice;
