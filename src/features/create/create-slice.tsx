import { FC } from 'react';
import { ICreateSliceProps } from 'features/create/config/create-type';

/**
 *
 */
const CreateSlice: FC<ICreateSliceProps> = () => {
    return (
        <div className="create-slice-wrapper">
            <div className="section">CreateSlice</div>
        </div>
    );
};

export default CreateSlice;
